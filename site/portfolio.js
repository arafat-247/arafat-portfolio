(()=>{'use strict';
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],root=document.body.dataset.root||'';
const menu=$('#mobile-menu'),toggle=$('.menutoggle'),backdrop=$('.menubackdrop'),drawerClose=$('.drawerclose');let closeTimer;
function openMenu(){clearTimeout(closeTimer);menu.hidden=false;backdrop.hidden=false;requestAnimationFrame(()=>{menu.classList.add('is-open');backdrop.classList.add('is-open')});toggle.setAttribute('aria-expanded','true');document.body.classList.add('menu-open');drawerClose.focus()}
function closeMenu(refocus=false){if(!menu)return;menu.classList.remove('is-open');backdrop.classList.remove('is-open');toggle?.setAttribute('aria-expanded','false');document.body.classList.remove('menu-open');closeTimer=setTimeout(()=>{menu.hidden=true;backdrop.hidden=true},260);if(refocus)toggle?.focus()}
toggle?.addEventListener('click',()=>toggle.getAttribute('aria-expanded')==='true'?closeMenu(true):openMenu());drawerClose?.addEventListener('click',()=>closeMenu(true));backdrop?.addEventListener('click',()=>closeMenu(true));menu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>closeMenu()));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu&&!menu.hidden)closeMenu(true)});
const current=location.pathname.split('/').pop()||'index.html';$$('.identity nav a,.mobilemenu>a').forEach(a=>{const href=a.getAttribute('href')||'';if(href===root+current||href.endsWith('/'+current))a.setAttribute('aria-current','page')});
const html=v=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function formatDate(v){if(!v)return'Date not recorded';const d=new Date(v);return Number.isNaN(+d)?'Date not recorded':d.toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric',timeZone:'Asia/Dhaka'})}
const archive=$('[data-archive]');
if(archive){const filterToggle=$('.filtertoggle');filterToggle?.addEventListener('click',()=>{const open=archive.classList.toggle('filters-open');filterToggle.setAttribute('aria-expanded',String(open))});(async()=>{try{
 const r=await fetch(root+'data/index.json');if(!r.ok)throw Error('Archive could not be loaded. Please reload.');
 const all=(await r.json()).articles.filter(a=>a.stream===archive.dataset.archive),q=$('[name=q]'),cat=$('[name=category]'),year=$('[name=year]');let page=1;
 $('.archive-total strong').textContent=all.length;
 [...new Set(all.map(a=>a.category).filter(Boolean))].sort().forEach(v=>cat.add(new Option(v,v)));[...new Set(all.map(a=>a.date_published?.slice(0,4)).filter(Boolean))].sort().reverse().forEach(v=>year.add(new Option(v,v)));
 const params=new URLSearchParams(location.search);q.value=params.get('q')||'';cat.value=params.get('category')||'';year.value=params.get('year')||'';
 function draw(){const term=q.value.trim().toLocaleLowerCase();const found=all.filter(a=>(!cat.value||a.category===cat.value)&&(!year.value||a.date_published.startsWith(year.value))&&[a.title,a.excerpt,a.category,a.source_name].join(' ').toLocaleLowerCase().includes(term));const pages=Math.max(1,Math.ceil(found.length/12));page=Math.min(page,pages);$('.count').textContent=found.length+' '+(found.length===1?'entry':'entries');
 $('.work-list').innerHTML=found.slice((page-1)*12,page*12).map(a=>{const url=/^(stories|thoughts)\/[a-zA-Z0-9_-]+\/$/.test(a.local_url)?root+a.local_url:root+'reporting.html';return `<article class="workitem"><div class="workmeta"><span>${html(formatDate(a.date_published))}</span><span>${html(a.category||'Reporting')}</span><span>${html(a.source_name||'Arafat Rahaman')}</span></div><div class="workcopy"><h2><a href="${html(url)}">${html(a.title)}</a></h2><p>${html(a.excerpt)}</p></div><a class="read" href="${html(url)}" aria-label="Read ${html(a.title)}"><span>Read</span> →</a></article>`}).join('')||'<p class="empty">No matches. Try another search or category.</p>';
 $('[data-page]').textContent=`Page ${page} of ${pages}`;$('[data-prev]').disabled=page<=1;$('[data-next]').disabled=page>=pages;const state=new URLSearchParams();if(q.value)state.set('q',q.value);if(cat.value)state.set('category',cat.value);if(year.value)state.set('year',year.value);history.replaceState(null,'',location.pathname+(state.size?'?'+state:''));
 }
 $('.tools').onsubmit=e=>{e.preventDefault();page=1;draw()};q.oninput=()=>{page=1;draw()};cat.onchange=year.onchange=()=>{page=1;draw()};$('[data-prev]').onclick=()=>{page--;draw();archive.scrollIntoView({behavior:'smooth'})};$('[data-next]').onclick=()=>{page++;draw();archive.scrollIntoView({behavior:'smooth'})};draw();
 }catch(e){$('.count').textContent=e.message;$('[data-next]').disabled=true}})()}
document.querySelector('[data-print]')?.addEventListener('click',()=>window.print());document.querySelector('[data-share]')?.addEventListener('click',async()=>{try{if(navigator.share)await navigator.share({title:document.title,url:location.href});else{await navigator.clipboard.writeText(location.href);$('[data-share-status]').textContent='Link copied.'}}catch(e){if(e.name!=='AbortError')$('[data-share-status]').textContent='Copy the address from your browser to share this story.'}});
let opener;const dialog=$('#photo-dialog');$$('[data-photo]').forEach(b=>b.onclick=()=>{opener=b;dialog.querySelector('img').src=root+b.dataset.photo;dialog.querySelector('img').alt=b.querySelector('img').alt;dialog.showModal()});$('[data-close-photo]')?.addEventListener('click',()=>dialog.close());dialog?.addEventListener('close',()=>opener?.focus());
if('IntersectionObserver'in window&&matchMedia('(min-width:721px) and (prefers-reduced-motion:no-preference)').matches){const items=$$('.tile,.workitem,.aboutlead,.coverage,.recognition li,.contactgrid>*');items.forEach(x=>x.classList.add('reveal'));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');observer.unobserve(e.target)}}),{rootMargin:'40px 0px'});items.forEach(x=>observer.observe(x))}
})();
