(()=>{
  const PER=20;let all=[],page=1,type='All',topic='',year='',order='latest';
  const terms=()=>document.querySelector('#archive-search').value.trim().toLowerCase().split(/\s+/).filter(Boolean);
  function score(a,ts){if(!ts.length)return 1;const fields=[[a.title,9],[a.topic,5],[a.story_type,4],[a.excerpt,2],[a.date_published,2]];return ts.reduce((sum,t)=>sum+fields.reduce((n,[v,w])=>n+(String(v||'').toLowerCase().includes(t)?w:0),0),0)}
  function filtered(){const ts=terms();return all.filter(a=>type==='All'||a.story_type===type).filter(a=>!topic||a.topic===topic).filter(a=>!year||String(a.date_published||'').startsWith(year)).map(a=>({a,s:score(a,ts)})).filter(x=>!ts.length||x.s>0).sort((x,y)=>ts.length?y.s-x.s||(order==='latest'?y.a.published_sort-x.a.published_sort:x.a.published_sort-y.a.published_sort):(order==='latest'?y.a.published_sort-x.a.published_sort:x.a.published_sort-y.a.published_sort)).map(x=>x.a)}
  function populate(){const ts=[...new Set(all.map(a=>a.topic).filter(Boolean).filter(t=>t!=='Public Affairs'))].sort(),ys=[...new Set(all.map(a=>String(a.date_published||'').slice(0,4)).filter(Boolean))].sort().reverse();document.querySelector('#topic-select').innerHTML='<option value="">All topics</option>'+ts.map(t=>`<option value="${AR.esc(t)}">${AR.esc(t)}</option>`).join('');document.querySelector('#year-select').innerHTML='<option value="">All years</option>'+ys.map(y=>`<option value="${y}">${y}</option>`).join('')}
  function item(a,i){const t=a.topic&&a.topic!=='Public Affairs'?`<a href="archive.html?topic=${encodeURIComponent(a.topic)}">${AR.esc(a.topic)}</a>`:'';return `<article class="archive-row-v9"><span class="archive-row-no-v9">${String(i+1).padStart(2,'0')}</span><time datetime="${AR.esc(a.date_published)}">${AR.esc(AR.fmtDate(a.date_published))}</time><div><div class="archive-row-meta-v9"><a href="archive.html?type=${encodeURIComponent(a.story_type)}">${AR.esc(a.story_type)}</a>${t}</div><h2><a href="${AR.esc(a.local_url)}">${AR.esc(a.title)}</a></h2>${a.excerpt?`<p>${AR.esc(AR.excerpt(a.excerpt,180))}</p>`:''}</div><a class="archive-row-arrow-v9" href="${AR.esc(a.local_url)}" aria-label="Read ${AR.esc(a.title)}">↗</a></article>`}
  function pages(n){const h=document.querySelector('#pagination');if(n<=1){h.innerHTML='';return}let out=`<button data-p="${page-1}" ${page===1?'disabled':''}>Previous</button>`;for(let p=Math.max(1,page-2);p<=Math.min(n,page+2);p++)out+=`<button data-p="${p}" class="${p===page?'active':''}">${p}</button>`;out+=`<button data-p="${page+1}" ${page===n?'disabled':''}>Next</button>`;h.innerHTML=out;h.querySelectorAll('[data-p]').forEach(b=>b.onclick=()=>{const v=+b.dataset.p;if(v<1||v>n)return;page=v;render();scrollTo({top:document.querySelector('.archive-results-v9').offsetTop-80,behavior:'smooth'})})}
  function render(){const list=filtered(),n=Math.max(1,Math.ceil(list.length/PER));page=Math.min(page,n);const start=(page-1)*PER;document.querySelector('#result-count').textContent=`${list.length} ${list.length===1?'story':'stories'}`;document.querySelector('#archive-grid').innerHTML=list.slice(start,start+PER).map((a,i)=>item(a,start+i)).join('');document.querySelector('#archive-empty').hidden=!!list.length;document.querySelector('#clear-all').hidden=type==='All'&&!topic&&!year&&!terms().length&&order==='latest';pages(n)}
  function fromURL(){const u=new URL(location.href);type=u.searchParams.get('type')||'All';topic=u.searchParams.get('topic')||'';year=u.searchParams.get('year')||'';document.querySelector('#archive-search').value=u.searchParams.get('q')||''}
  function sync(){const u=new URL(location.href);const set=(k,v,d='')=>v&&v!==d?u.searchParams.set(k,v):u.searchParams.delete(k);set('type',type,'All');set('topic',topic);set('year',year);set('q',document.querySelector('#archive-search').value.trim());history.replaceState(null,'',u)}
  async function init(){try{({articles:all}=await AR.load());fromURL();populate();document.querySelector('#topic-select').value=topic;document.querySelector('#year-select').value=year;document.querySelectorAll('.archive-type').forEach(b=>{b.classList.toggle('active',b.dataset.type===type);b.onclick=()=>{type=b.dataset.type;page=1;document.querySelectorAll('.archive-type').forEach(x=>x.classList.toggle('active',x===b));sync();render()}});document.querySelector('#topic-select').onchange=e=>{topic=e.target.value;page=1;sync();render()};document.querySelector('#year-select').onchange=e=>{year=e.target.value;page=1;sync();render()};document.querySelector('#sort-select').onchange=e=>{order=e.target.value;page=1;render()};document.querySelector('#archive-search').oninput=()=>{page=1;sync();render()};document.querySelector('#clear-all').onclick=()=>{type='All';topic=year='';order='latest';document.querySelector('#archive-search').value='';document.querySelector('#topic-select').value='';document.querySelector('#year-select').value='';document.querySelector('#sort-select').value='latest';document.querySelectorAll('.archive-type').forEach(b=>b.classList.toggle('active',b.dataset.type==='All'));sync();render()};addEventListener('keydown',e=>{if(e.key==='/'&&!/INPUT|TEXTAREA|SELECT/.test(document.activeElement.tagName)){e.preventDefault();document.querySelector('#archive-search').focus()}});render()}catch(e){console.error(e);document.querySelector('#result-count').textContent='Archive failed to load';document.querySelector('#archive-grid').innerHTML='<p class="archive-empty-v9">Please refresh the page or try again shortly.</p>'}}
  init();
})();
