(()=>{async function init(){try{const data=await AR.json('data/photography.json'),photos=data.photos||[],grid=document.querySelector('#photo-grid'),empty=document.querySelector('#photo-empty');if(!photos.length){empty.hidden=false;return}empty.hidden=true;grid.innerHTML=photos.map((p,i)=>`<figure class="photo-item-v9 ${i%5===0?'wide':''}"><a href="${AR.esc(p.full||p.src||'#')}" target="_blank" rel="noopener"><img src="${AR.esc(p.src)}" alt="${AR.esc(p.alt||p.caption||'Photograph by Arafat Rahaman')}" loading="lazy" decoding="async"></a><figcaption><span>${AR.esc(p.caption||'')}</span><small>${AR.esc([p.location,p.year].filter(Boolean).join(' · '))}</small></figcaption></figure>`).join('')}catch(e){console.error(e)}}init()})();
